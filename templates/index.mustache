{{> header }}
	<div class="wrapper">
	{{#preview}}	
	<a href="/{{label}}" class="preview" id="{{label}}">
		<article>
			<h3>{{title}}</h3>
			<p>{{info}}</p>
			<p>{{tag}}</p>
		</article>
	</a>		
	{{/preview}}
	</div>
{{> footer }}
