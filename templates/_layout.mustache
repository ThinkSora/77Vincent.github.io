{{> header }}
	<div class="wrapper">
		{{#section}}
		<section id="{{type}}">
			{{#preview}}	
			<a href="{{href}}" class="preview jsColor" id="{{name}}">
				<div class="{{profile}}"></div>

				<article class="">
					<h1 class="">{{title}}</h1>
					<p>{{info}}</p>
				</article>
			</a>		
			{{/preview}}
		</section>
		{{/section}}
	</div>
{{> footer }}
