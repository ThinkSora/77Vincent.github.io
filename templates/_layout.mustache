{{> header }}
	<div class="wrapper">
		{{#section}}
		<section id="{{type}}">
			{{#preview}}	
			<a href="{{href}}" class="preview jsColor" id="{{name}}">
				<article class="">
					<h1 class="">{{title}}</h1>
					<p>{{info}}</p>
					<aside>
						<p>{{date}}</p>
						<p>{{category}}</p>
					</aside>
				</article>
			</a>		
			{{/preview}}
		</section>
		{{/section}}
	</div>
{{> footer }}
